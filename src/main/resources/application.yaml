spring:
  datasource:
    url: "jdbc:mysql://localhost:3308/identity_service"
    username: root
    password: root
  application:
    name: identity-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
  servlet:
    context-path: /identity