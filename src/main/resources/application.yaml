spring:
  datasource:
    url: "jdbc:mysql://localhost:3308/tuancui_test"
    username: root
    password: root
  application:
    name: identity-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080
  servlet:
    context-path: /api